# src/main/resources/application.yml

spring:
  datasource:
    # [정보은닉] DB 연결 정보 등 민감한 데이터소스 설정
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vibevoca?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: khs10914

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true # 콘솔에 SQL 로그를 출력합니다.
    properties:
      hibernate:
        format_sql: true # 출력되는 SQL 쿼리를 가독성 좋게 정렬합니다.
        highlight_sql: true # SQL 예약어 등에 색상을 입혀 시각적으로 구분합니다.